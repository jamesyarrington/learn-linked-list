namespace LinkedList
{
    class LinkedList<T>
    {
        private Node<T> head;
        private Node<T> tail;

        public void AddNode(Node<T> node)
        {

        }

        public override string ToString()
        {
            return "foo";
        }
    }
}
